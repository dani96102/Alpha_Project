# Dependencies
/node_modules

# Environment Variables (VERY IMPORTANT!)
.env
.env.*
!.env.example

# Log files
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Build output folders (if you have a build step)
/dist
/build
/out

# OS-specific files
.DS_Store
Thumbs.db

# Editor directories and files
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json
*.sublime-workspace
.idea

# Firebase Service Account Key (If stored locally - NOT RECOMMENDED)
# *.json # Be very careful with this, might ignore wanted JSON files!
# serviceAccountKey.json # Better to be specific if you MUST store it (don't!)

# Other generated files
coverage/
*.tgz # npm pack files

# Firebase Hosting temp files
.firebase/